# Feature Spec: 北陆图书馆

> 此文件由 Git Worktree Design Skill 自动产生，供 AI Agent 作为开发指引。

## 分支资讯

| 项目 | 值 |
|------|-----|
| 分支名称 | `feature/north-library` |
| 基于分支 | `main` |
| Worktree 路径 | `E:\Workplace\Learn_project\get_genshin_wiki-north-library` |
| 建立时间 | 2026-05-13 |

## 目标

爬取并解析原神 Wiki 中的北陆图书馆中关于游戏的百科数据(https://wiki.biligame.com/ys/北陆图书馆)，数据的结构按百科目录的格式组织。

### 目录章节对应法则  
| 目录标题等级 | 格式示例 | `action=raw`中对应格式示例 | 说明 |
|------|------|------|------|
| 一级 | 1 | =<center>提瓦特</center>= | 位于两个`=`中间，`<center>`只是用于居中表示和标题等级无关 |
| 二级 | 1.1 | ==[[提瓦特编年史]]==、==降临者== | 位于两个`==`中间，用于表示二级标题，`[[]]`只是链接不包含标题内容 |
| 三级 | 1.2.1 | ===穿越星海=== | 位于两个`===`中间，用于表示三级标题 |
| 四级 | 1.4.1.1 | ====史莱姆==== | 位于两个`====`中间，用于表示四级标题 |
| 项目 | 无 | '''时间''' | 位于两个`'''`中间，用于表示项目标题 |
| 条目 | 无 | *来自世界之外且唯有自身等价一个世界、能改变世界命运的人才能担此头衔 | 位于`*`后面，类似于markdown的`-`的作用 |
| 正文 | 无 | 各级标题后面的内容 | 正文内容 |

*注意*：
- 通过 `https://wiki.biligame.com/ys/北陆图书馆` 逐级检各级标题
- 所有项目都需要提取验收
- 数据源为 `https://wiki.biligame.com/ys/北陆图书馆`，内容为中文
- 优先使用 `mwparserfromhell` 解析 wikitext，无法处理时使用正则或手动解析
- 对话内容中的 `<br>` 替换为 `\n`

## 实作范围

- [x] 确定 Wiki 分类名称（预计 `北陆图书馆` 或类似，需实际探测）
- [x] 在 `parser.py` 实现百科页面解析逻辑
- [x] 实现爬取与持久化流程
- [x] 编写测试用例验证解析结果

## 验收标准

- 能够成功抓取北陆图书馆各级标题下的所有数据
- 解析结果包含各级标题的结构与内容正文
- 数据可正确序列化为 JSON 并持久化

## 技术约束

- 遵循 `robots.txt` 规则
- 使用 `mediawiki API` 获取列表、页面 `json` 数据
- 使用 `mwparserfromhell` 解析 `json` 数据
- 复用现有 `WikiCrawler`、`MediaWikiClient` 等基础设施
- 复用现有 `BookRecord` / `BookVolume` 模型

## 跨分支备注

与其他 worktree 分支无依赖，可独立开发与合并。