# Feature Spec: 提瓦特编年史（公元纪）

> 此文件由 Git Worktree Design Skill 自动产生，供 AI Agent 作为开发指引。

## 分支资讯

| 项目 | 值 |
|------|-----|
| 分支名称 | `feature/teyvat-chronicle` |
| 基于分支 | `main` |
| Worktree 路径 | `E:\Workplace\Learn_project\get_genshin_wiki-teyvat-chronicle` |
| 建立时间 | 2026-05-13 |

## 目标
- 从 `https://wiki.biligame.com/ys/提瓦特编年史` 抓取提瓦特编年史相关页面
记录包括总览(提瓦特)、各国(蒙德、璃月、稻妻、须弥、枫丹、纳塔、至冬、坎瑞亚)、其他地区(白夜国、星球、宇宙)等。  

### 编年史章节对应法则  
| 目录标题等级 | `action=raw`中对应格式示例 | 说明 |
|------|------|------|
| 一级 | 无该等级 |  |
| 二级 | 无该等级 |  |
| 三级 | === 太古 === | 位于两个`===`中间，用于表示三级标题 |
| 四级 | ====原初==== | 位于两个`====`中间，用于表示四级标题 |
| 五级 | =====原初之人===== | 位于两个`=====`中间，用于表示五级标题 |
| 项目 | '''时间''' | 位于两个`'''`中间，类似于markdown的`**`的加粗的作用 |
| 条目 | *来自世界之外且唯有自身等价一个世界、能改变世界命运的人才能担此头衔 | 位于`*`后面，类似于markdown的`-`的作用 |
| 正文 | 各级标题后面的内容 | 正文内容 |

*注意*：
- 记录的json数据按目录标题等级的结构组织
- 通过 `https://wiki.biligame.com/ys/提瓦特编年史` 逐级检各级标题
- 所有项目都需要提取验收
- 数据源为 `https://wiki.biligame.com/ys/提瓦特编年史`，内容为中文
- 各国(蒙德、璃月、稻妻、须弥、枫丹、纳塔、至冬、坎瑞亚)、其他地区(白夜国、星球、宇宙)等通过`https://wiki.biligame.com/ys/xxxxx`(其中xxxxx为国家/地区名称的中文编码)。
- 优先使用 `mwparserfromhell` 解析 wikitext，无法处理时使用正则或手动解析
- 对话内容中的 `<br>` 替换为 `\n`

## 实作范围

- [ ] 确定 Wiki 分类名称（实际未发现专用分类，回退探测页面 `提瓦特编年史（公元纪）`）
- [ ] 在 `parser.py` 实现编年史页面解析逻辑
- [ ] 实现爬取与持久化流程
- [ ] 编写测试用例验证解析结果

## 验收标准

- 能够成功抓取提瓦特编年史分类下的所有页面，包括总览(提瓦特)、各国(蒙德、璃月、稻妻、须弥、枫丹、纳塔、至冬、坎瑞亚)、其他地区(白夜国、星球、宇宙)等
- 解析结果包含年代/事件结构化信息(各级标题中包含的时间信息)
- 数据可正确序列化为 JSON 并持久化

## 技术约束

- 遵循 `robots.txt` 规则
- 使用 `mediawiki API` 获取列表、页面 `json` 数据
- 使用 `mwparserfromhell` 解析 `json` 数据
- 复用现有 `WikiCrawler`、`MediaWikiClient` 等基础设施

## 跨分支备注

与其他 worktree 分支无依赖，可独立开发与合并。
