---
项目名称：get_wiki_genshin
项目目标：获取B站原神wiki上的全部角色、剧情、任务等数据的文本、图片等数据，完成页面下载、解析、清洗、结构化存储，并为后续 RAG / Agent 训练提供高质量数据集。

AGENT 的任务不是“随便写能跑的脚本”，而是编写：
- 可维护
- 可扩展
- 可测试
- 结构清晰
- 符合项目数据规范的工程代码。
---

# 1. 数据源的有关信息
+ 数据源：[B站原神wiki主页](https://wiki.biligame.com/ys/%E9%A6%96%E9%A1%B5)  
+ 该wiki基于MediaWiki协议编写，因此可以使用MediaWiki的API获取数据
+ 该wiki的页面结构相对规范，主要包含角色、剧情、任务等类型的页面，每个页面都有相似的HTML结构，便于解析。

# 2. 项目构建规则
## 核心原则
0. 绝对合规性原则：每次爬取数据等待5s时间，避免对目标服务器造成过大压力。
1. 优先保证正确性，再考虑性能。
2. 优先写清晰、可维护的代码，不写炫技代码。
3. 避免把逻辑写死在单个脚本中，尽量模块化。
4. 任何抓取、解析、清洗、存储逻辑都要可单独测试。
5. 任何新增功能必须考虑异常处理、日志、可复用性。
6. 不要擅自引入与当前任务无关的大型依赖。
7. 代码要尽量符合 Python 社区惯例。

## 技术约束
- 语言：Python 3.10+
- 网络请求：优先使用 `requests`
- HTML 解析：优先使用 `BeautifulSoup`
- Wikitext 解析：优先使用 `mwparserfromhell`
- 配置管理：优先使用 `.env`、`yaml` 或 `toml`
- 数据存储：优先使用 `jsonl`、`json`、`sqlite`
- 日志：使用标准库 `logging`
- 测试：使用 `pytest`  

如项目已有既定技术栈，必须优先遵循现有栈。

## 项目目录约定

如果仓库中没有明确结构，AGENT 应优先采用以下布局：
```text
get_genshin_wiki/
├── src/
│   ├── crawler/        # 页面发现、API 请求、分页、重试
│   ├── parser/         # wikitext / html 解析
│   ├── cleaner/        # 文本清洗与标准化
│   ├── storage/        # 文件、sqlite、jsonl 存储
│   ├── schema/         # 数据结构定义
│   └── utils/          # 通用工具
├── tests/
├── data/
├── configs/
├── scripts/
├── main.py
└── AGENTS.md
```
如果现有项目结构不同，AGENT 应优先遵循现有结构，不要擅自重构整个仓库。


## 数据采集目标
优先采集并保留以下字段： 
- 页面标题
- 页面 URL
- 页面 ID
- 命名空间
- 最近修改时间
- 原始 wikitext
- 清洗后的纯文本
- 分类信息
- 内部链接
- 模板信息
- Infobox 信息
- 页面摘要或首段
- 抓取时间

如页面存在结构化信息，应优先将其提取为结构化字段，而不是只保留纯文本。  

## MediaWiki 抓取原则
1. 优先使用 API  
优先通过 MediaWiki API 获取页面、分类、修订、模板、链接等信息，不要默认使用 HTML 爬虫。  

2. 支持分页  
所有带 continuation 的接口都必须完整处理分页，不能只抓第一页。

3. 控制频率  
必须设置请求间隔、重试和退避机制，避免对目标站点造成压力。

4. 保留来源信息  
每条记录都应包含来源页面、来源 URL、抓取时间和抓取方式。

5. 增量更新  
如果接口支持最近变更或修订信息，优先支持增量抓取，而不是每次全量重抓。  



## 页面发现策略
AGENT 在发现页面时，应优先考虑以下来源：
- 分类页面（Category）
- 页面列表页
- 最近更改
- 站内搜索结果
- 入口索引页

如用户明确指定抓取范围，应严格遵循指定范围，不得擅自扩大抓取面。  

## 解析要求
### Wikitext
- 保留原始 wikitext
- 再解析出结构化信息
- 不要直接丢弃模板、链接、分类等信息


### Infobox
如页面包含 Infobox，必须尽量提取：
- 模板名
- 键
- 值
- 原始模板文本
- 链接
- 内链应单独提取
- 去重
- 忽略明显无效或噪声链接
- 分类
- 分类应标准化保存
- 保留原始分类名
- 支持去重和层级关系

### 文本清洗
清洗时应去除：  
- 多余空白
- 引用脚注噪声
- 明显残留的模板标记
- 重复标点
- 
但不得过度清洗到丢失语义信息。


## 数据存储要求

优先输出以下格式之一：

- jsonl：适合批量数据集
- json：适合结构化对象
- sqlite：适合中小规模索引查询

每条记录至少包含：
``` json
{
  "id": "...",
  "title": "...",
  "url": "...",
  "namespace": 0,
  "source": "mediawiki",
  "fetched_at": "...",
  "content_raw": "...",
  "content_clean": "...",
  "categories": [],
  "links": [],
  "templates": [],
  "infobox": {},
  "revision": {}
}
```
原始数据与清洗结果必须分开保存，不能互相覆盖。

## 错误处理要求

必须处理以下异常场景：

- 网络超时
- HTTP 错误
- JSON 解析失败
- 空页面
- 缺失字段
- 分页失败
- 限速或临时封禁
- 页面结构变化
- 模板解析失败

不要使用裸 except:。应捕获明确异常类型并记录日志。

## 重试与限速
- 对临时网络错误应重试
- 对 429 / 5xx 错误应退避重试
- 所有请求应有限速
- 必要时应支持可配置的 sleep_interval
- 必须设置合理的 User-Agent

## 日志要求

关键步骤必须记录日志，包括：

- 开始抓取
- 页面发现
- 页面下载
- 解析成功 / 失败
- 存储成功 / 失败
- 分页继续
- 重试与退避

日志应便于排查问题，但不要输出敏感信息。


## 测试要求
每个子任务必须先进行单独测试。

任何新增核心逻辑都必须考虑测试，至少应覆盖：
- 正常路径
- 空输入
- 缺失字段
- 分页处理
- 解析失败
- 存储失败
- 清洗结果正确性  
  
如无法立即写完整测试，至少要提供可运行的最小样例。

## 编码规范
- 函数职责单一
- 名称清晰
- 使用类型注解
- 使用 docstring 说明复杂逻辑
- 避免魔法常量散落
- 避免重复代码
- 避免把解析、抓取、存储混写在一个函数里

# 类的使用原则
只有在以下场景才优先使用类：
- 需要维护会话状态
- 需要封装 API 客户端
- 需要统一管理存储器
- 需要封装解析器

推荐类名示例：
- MediaWikiClient
- PageFetcher
- WikitextParser
- DatasetWriter


## 修改现有代码时的原则
当需要修改已有模块时：
- 先阅读相关代码和调用链
- 不要破坏现有接口
- 尽量保持向后兼容
- 只改与任务直接相关的部分
- 如需重构，先给出最小可行方案

## 工作流程
AGENT 在开始编码前，应按顺序执行：
1. 理解任务目标
2. 找到相关文件
3. 明确输入输出
4. 确认抓取范围
5. 设计数据结构
6. 编写或修改代码
7. 补充测试
8. 检查格式与错误处理
9. 给出简要变更说明


## 输出要求
任务完成后，AGENT 应说明：
- 做了什么
- 修改了哪些文件
- 为什么这样设计
- 有哪些风险或限制
- 如何验证结果


## 禁止事项
- 不要凭空编造接口行为
- 不要删除原始数据
- 不要跳过异常处理
- 不要把临时调试代码留在正式实现中
- 不要过度设计
- 不要在没有明确需求时大改项目结构
- 不要默认抓取用户未授权的内容


## MediaWiki 项目专项要求
如果任务与 MediaWiki 有关，AGENT 还必须遵守：
- 优先使用 API
- 处理 continue 分页
- 处理 categorymembers
- 处理 revisions
- 处理 templates
- 处理 links
- 尽量提取 Infobox
- 保留原始 wikitext 和解析结果
- 为后续 RAG / 知识图谱 / 训练数据预留元信息


# 完成标准
只有满足以下条件时，任务才算完成：
- 代码可运行
- 核心逻辑完整
- 错误处理合理
- 关键功能有测试或最小验证样例
- 输出结构符合项目要求
- 结果有清晰说明