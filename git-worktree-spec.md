# Feature Spec: 怪物

> 此文件由 Git Worktree Design Skill 自动产生，供 AI Agent 作为开发指引。

## 分支资讯

| 项目 | 值 |
|------|-----|
| 分支名称 | `feature/monsters` |
| 基于分支 | `main` |
| Worktree 路径 | `E:\Workplace\Learn_project\get_genshin_wiki-monsters` |
| 建立时间 | 2026-05-06 |

## 目标

新增怪物的数据模型与解析器，实现怪物类型、属性、掉落物品、出现地点等关键字段的结构化提取。

具体**需要获取的内容**：  
1. 以怪物 `门扉前的弈局` 为例：
| 验收 | 项目 | 内容 | 注释 |
|------|------|------|-----|
| [ ] | 怪物名 | 门扉前的弈局 |  |
| [ ] | 怪物类别 | 周刷BOSS |  |
| [ ] | 怪物分类 | 值得铭记的强敌 |  |
| [ ] | 怪物类型 | 其他 |  |
| [ ] | 出现地点 | 蒙德·风起地 |  |
| [ ] | 掉落素材 | BOSS | 若`掉落素材=BOSS`，则`掉落素材`项填写`BOSS素材`，本案例中为`升扬样本·骑士`、`升扬样本·战车`、`升扬样本·王族` |
| [ ] | 介绍 | 集魔女会诸家技艺而制成的集团军。\n虽然全都……(省略) |  |

2. 以怪物 `遗迹防卫者` 为例：
| 验收 | 项目 | 内容 | 注释 |
|------|------|------|-----|
| [ ] | 怪物名 | 遗迹防卫者 |  |
| [ ] | 怪物类别 | 精英 |  |
| [ ] | 怪物分类 | 自律机关 |  |
| [ ] | 怪物类型 | 战争机械 |  |
| [ ] | 出现地点 | 稻妻 |  |
| [ ] | 掉落素材 | `混沌机关`、`混沌枢纽`、`混沌真眼` | 若`掉落素材=BOSS`，则`掉落素材`项填写`掉落素材`中除了`摩拉`外的素材 |
| [ ] | 介绍 | 为了适应特殊的目标，有着定制化外形与机能的异形机械。\n传说是已经覆灭的国度留下的战……(省略) |  |

*注意*：
- 所有项目都需要提取验收
- 介绍的换行使用`\n`表示，将每一个`<br>`都替换成`\n`，若有两个`<br>`连续则替换成两个连续的`\n`

## 实作范围

- [x] 新增 `MonsterRecord` dataclass（名称、类型、属性、掉落物品、出现地点等）
- [x] 新增 `parse_monster_page(payload)` 解析方法
- [x] 新增 CLI 子命令 `parse monster <name>`
- [x] 新增测试覆盖
- [x] 更新 `models.py` 新增 MonsterRecord
- [x] 更新 `parser.py` 新增解析方法
- [x] 更新 `cli.py` 新增命令

## 验收标准

- 能正确提取怪物名称、类型、属性、掉落物品、出现地点
- 解析结果符合 `MonsterRecord` dataclass 结构

## 技术约束

- 不得引入新的 pip 依赖
- 需兼容现有的 `MediaWikiClient`、`WikiCrawler`、`JsonFileStore` 接口
- 遵循既有的代码风格与模块结构

## 跨分支备注

- 建议第二个合并
- 合并时需注意 `models.py` 中 `MonsterRecord` 命名不与其他分支冲突
